// 스코프 
// 변수가 유효되는 범위

// 전역 스코프 - 코드 어디에서나 참조 가능 
// 지역 스크프 - 함수 코드 블록이 만든 스코프로 함수 자신과 하위 함수에서만 참조할 수 있다.

// 전역 변수 - 전역에서 선언된 변수이며 어디에든 참조할 수 있다.
// 지역 변수 - 지역(함수) 내에서 선언된 변수이며 지역 안에서만 참조할 수 있다.

let global = 30;

function scope(){
    let local = 20;
    console.log(global);
}

scope(); // 30
console.log(local); err

global 부분이 전역 scope 부분이 지역이다.

// 조건문, 반복문 {}안에 있는 범위를 블록 - let, const
// 함수형 안에 있는 것을 함수 - var(블록스코프 무시함.)

// 스코프체이닝 - 식별자를 찾을 때, 자신이 속한 스코프에서 먼저 찾고, 자신이 속한 스코프에 없다면 상위스코프에서 다시 찾아나간다.
// 스코프체인 - 식별자를 결정할 때 활용하는 스코프들의 연결리스트를 스코프 체인 이라고한다.

// 렉시컬 스코프 - 함수를 어디서 선언하였는지에 따라 상위 스코프를 결정하는 것이다.