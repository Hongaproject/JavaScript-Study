// 이벤트 전파, 위임, 캡처링/버블링
// 이벤트 전파 - DOM트리 상에 존재하는 DOM요소 노드에서 이벤트 발생시 DOM트리를 통해 다른 DOM요소 노드로 전파되는 것을 뜻함.

// 이벤트 위임 - 하위 요소에서 각각 이벤트를 붙이지 않고 상위 요소에서 하위 요소의 이벤트들을 제어하는 방식
// 사용시 요소마다 핸들러를 항당하지 않고 공통 조상에 이벤트 핸들러를 하나만 할당해도 여러 요소를 한 번 에 다룰 수 있습니다.

// 이벤트 캡처링 - 이벤트가 하위요소로 전파되는 단계

// 이벤트 버블링 - 이벤트가 상위요소로 전파되는 단계