// 함수선언
// function 함수명 ()으로 선언이되며 코드를 실행전에 로드가 되며 호이스팅이 일어난다.

// 함수표현식
// const 함수명 = function ()으로 선언이되며 코드를 실행하면 로드가 되어서 호이스팅이 일어나지 않는다.

// 일반 함수
// function 함수명 ()으로 선언이되며 this가 어떻게 호출이 되냐에 따라 바인딩할 객체가 동적으로 결정이된다.

// 화살표 함수
// 화살표를 사용해서 일반 함수보다 간편하게 사용이 가능하고 this는 항상 상위 스코프를 가르키기 때문에 바인딩할 객체가 정적으로 결정된다.
